$(document).ready(function() {
$( ".box-shadow-menu" ).click(function() {
			if ($("#nav-menu").is(":visible")) {
				$("#nav-menu").hide(500);
			} else {
				$("#nav-menu").show(500);
			}
		
		});
		
});